version: '3'

services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: revving
      POSTGRES_USER: colinkingswood
      POSTGRES_PASSWORD: testpassword
    volumes:
      - postgres_data:/var/lib/postgresql/data

  web:
    build:
      context: .  # Assuming the Dockerfile is in the root directory
      dockerfile: Dockerfile-django  # Specifically using the development
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
#      - db
      - redis

  migrate:
    build: .
    command: python manage.py migrate
    volumes:
      - .:/app
    depends_on:
      - db
  redis:
    image: redis:alpine


#  celery:
#    build:
#      context: .  # Assuming the Dockerfile is in the root directory
#      dockerfile: Dockerfile-celery  # Specifically using the development
#    command: celery -A revving worker --loglevel=info
#    volumes:
#      - .:/code
#    depends_on:
#      - db
#      - redis


volumes:
  postgres_data:
